# Test dependencies. Runtime deps live in requirements.txt and are not
# duplicated here, so install both to run the suite:
#   pip install -r backend/requirements.txt -r backend/requirements-dev.txt
pytest>=8.0.0
# Required by fastapi.testclient.TestClient, which is built on httpx.
httpx>=0.27.0
